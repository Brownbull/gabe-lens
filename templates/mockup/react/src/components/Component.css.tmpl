/*
 * {{COMPONENT_NAME}} — React-side stylesheet.
 *
 * IMPORTANT: do NOT redeclare token values here. The canonical mockup CSS
 * (loaded via src/styles/tokens.css → @mockups/assets/css/...) already
 * provides every var(--*) we use. This file ONLY contains layout / animation
 * rules that mirror the HTML mockup's component CSS.
 *
 * If you find yourself reaching for a hex literal, you have drift. Stop and
 * find the right token in the canonical CSS instead.
 *
 * Loaded as a side-effect import (not CSS Modules) — class names match the
 * HTML mockup verbatim so React JSX and HTML mockup share the same selectors.
 */

{{COMPONENT_CSS_BODY}}
