/*
 * Token chain — emitted by /gabe-mockup spike scaffolding.
 *
 * Re-imports the canonical mockup tokens CSS so React components consume the
 * SAME `--bg`, `--ink`, `--primary`, etc. variables that the static HTML
 * mockups consume. Switching `[data-theme][data-mode]` on <body> cascades
 * through both surfaces simultaneously. SINGLE SOURCE OF TRUTH — never
 * redeclare token values here.
 *
 * Path resolves through the Vite alias `@mockups` configured in vite.config.ts.
 */
@import "@mockups/assets/css/{{TOKENS_FILENAME}}";
